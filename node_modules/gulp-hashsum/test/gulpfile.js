// including plugins
var gulp = require("gulp");
var hashsum = require("./index");

gulp.task("default", function() {
  gulp
    .src("*.js")
    .pipe(
      hashsum({
        stream: true
      })
    )
    .pipe(gulp.dest("test"));
});
